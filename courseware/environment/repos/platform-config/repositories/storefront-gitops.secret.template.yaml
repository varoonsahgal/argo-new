# TEMPLATE — the storefront-gitops repository connection Secret (Lab 2 E1).
#
# PLACEHOLDERS only. In Lab 2 the participant renders this by substituting:
#   <PASSWORD>  the Gitea password for user `student`, read from
#               ~/course/credentials/gitea-student.txt (never committed).
apiVersion: v1
kind: Secret
metadata:
  name: repo-storefront-gitops
  namespace: argocd
  labels:
    argocd.argoproj.io/secret-type: repository
type: Opaque
stringData:
  type: git
  url: http://lab-gitea:3000/course/storefront-gitops.git
  username: student
  password: <PASSWORD>
